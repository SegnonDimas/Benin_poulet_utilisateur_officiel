class ModelPresentationPage {
  final String title, description, image, nextMsg, descriptionTitle;
  ModelPresentationPage({
    required this.title,
    required this.description,
    required this.image,
    required this.nextMsg,
    required this.descriptionTitle,
  });
}
